import { Header } from "../../components/Header"
import { Summary } from "../../components/Summary"

export const Trasactions = () => {
  return(
    <div>
      <Header/>
      <Summary/>
    </div>
  )
}